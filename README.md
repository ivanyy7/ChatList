# ChatList - Сравнение ответов нейросетей

Приложение для отправки одного промпта в несколько нейросетей и сравнения их ответов.

## Возможности

- 📝 Отправка промпта в несколько моделей одновременно
- 💾 Сохранение промптов и результатов в базе данных SQLite
- 🔍 Поиск и сортировка по промптам, моделям и результатам
- 📤 Экспорт результатов в Markdown и JSON
- 📊 Управление моделями нейросетей через удобный интерфейс
- 🔐 Безопасное хранение API-ключей в файле `.env`
- 📝 Логирование всех запросов и действий

## Требования

- Python 3.11+
- PyQt5
- requests
- python-dotenv

## Установка

1. Клонируйте репозиторий или скачайте файлы проекта

2. Установите зависимости:

```powershell
pip install -r requirements.txt
```

3. Настройте файл `.env`:

Скопируйте `.env.example` в `.env` и заполните своими API-ключами:

```env
OPENAI_API_KEY=your-openai-api-key
DEEPSEEK_API_KEY=your-deepseek-api-key
GROQ_API_KEY=your-groq-api-key
OPENROUTER_API_KEY=your-openrouter-api-key
HUGGINGFACE_API_KEY=your-huggingface-api-key
```

## Запуск

```powershell
python main.py
```

## Использование

### Добавление моделей

1. Откройте меню **"Модели"** → **"Управление моделями"**
2. Нажмите **"Добавить"**
3. Заполните форму:
   - **Название модели**: например, "GPT-4" или "OpenRouter GPT-4"
   - **URL API**: URL для отправки запросов (например, `https://openrouter.ai/api/v1/chat/completions`)
   - **API ID**: Имя переменной окружения с API-ключом (например, `OPENROUTER_API_KEY`)
   - **Активна**: Отметьте, если модель должна использоваться по умолчанию

### Отправка запросов

1. Введите промпт в текстовое поле
2. (Опционально) Добавьте теги для категоризации
3. Нажмите кнопку **"Отправить"**
4. Дождитесь получения ответов от всех активных моделей
5. Выберите результаты, которые хотите сохранить (чекбоксы)
6. Нажмите **"Сохранить выбранные"**

### Просмотр истории

- **Промпты**: Меню **"Промпты"** → **"История промптов"**
- **Результаты**: Меню **"Результаты"** → **"Сохранённые результаты"**

### Экспорт результатов

1. После получения результатов откройте меню **"Результаты"** → **"Экспорт"**
2. Выберите формат (Markdown или JSON)
3. Укажите путь для сохранения файла

## Поддерживаемые API

- OpenAI API
- DeepSeek API
- Groq API
- OpenRouter API
- HuggingFace API
- Универсальный формат (совместимый с OpenAI)

## Структура проекта

```
ChatList/
├── main.py              # Главный файл с GUI
├── db.py                # Модуль работы с базой данных
├── models.py            # Модуль работы с моделями
├── network.py           # Модуль сетевых запросов
├── config.py            # Модуль конфигурации
├── export.py            # Модуль экспорта результатов
├── logger.py            # Модуль логирования
├── .env                 # Файл с API-ключами (не коммитится)
├── .env.example         # Шаблон файла .env
├── requirements.txt     # Зависимости проекта
└── chatlist.db          # База данных SQLite (создаётся автоматически)
```

## База данных

Приложение использует SQLite для хранения данных. База данных создаётся автоматически при первом запуске.

### Таблицы:

- **prompts** - сохранённые промпты
- **models** - конфигурация моделей нейросетей
- **results** - сохранённые результаты ответов
- **settings** - настройки приложения

Подробнее см. `DATABASE.md`

## Логирование

Все запросы и действия логируются в файл `chatlist.log`. Логи ротируются при достижении размера 10 МБ (сохраняется до 5 резервных копий).

## Настройки

- **Отключение звуков**: Меню **"Настройки"** → **"Отключить звуки"**
- Уровень логирования можно изменить через таблицу `settings` в БД (ключ `log_level`)

## Создание исполняемого файла

```powershell
pyinstaller --onefile --windowed --name="ChatList" main.py
```

Исполняемый файл будет создан в папке `dist`.

## Лицензия

Проект создан для личного использования.

## Поддержка

При возникновении проблем проверьте:
1. Правильность API-ключей в файле `.env`
2. Наличие активных моделей в базе данных
3. Логи в файле `chatlist.log`
